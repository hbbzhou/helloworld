Lua代码检查用例

--
需要有数据保存后, 一致性校验的函数
过期数据清空
return逻辑都需要 debug日志, 尤其涉及奖励逻辑 ; 以及 确保返回值 正确
正则_Lua点号->[^(Game)(this)(g)(pb)(json)(.)][.][a-zA-Z]{1,30}[(]
出现 :: 和  self:m
self.m_qwUserId 于self.m_userId 混淆
初始化时, 调用了B模块中的变量;(但是B模块还未初始化)
弄错参数类型了--GM中参数是字符串
判断条件弄反了(not 后面一定是return false) (必须要有注释)
玩家等级不满足配置要求
变量赋值逻辑正确(写代码的时候确保)
--


--合并代码_检查用例
--
修改if冲突,少个end
修改elseif 多个end
--









